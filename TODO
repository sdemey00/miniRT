Mandatory :
- PDF
- Remove file wildcard in Makefile
- Norm
- Deep test parsing
	- Check input ranges

Bonus :
- Bump map textures

Bonus ++ :
- Add objects to the scene
- Rendering load bar on screen.

FIX :
- Shadow have the color of emiting light. Investigate ?
- No lights when 2 planes above each other ? Not able to make a closed room ?
Shadows might overflow the world ?

sdemey :
- Fix checkerboard pattern on cylinder when rotated.
- Surface normal and uv coordinates for the cone.

mmichele :
- Remove object from the scene
- Be able to cycle trough the light with the arrows.
- Optimize blur to full rendering, with an array of booleans to remember, which
pixel has already been rendered.
- Rotation of an object does not rotates as expected.
